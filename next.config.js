/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['site.ieee.org', 'avatars.githubusercontent.com','drive.google.com'],
  },
}

module.exports = nextConfig
